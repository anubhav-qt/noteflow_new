{
  "name": "noteflow",
  "version": "1.0.0",
  "description": "An application that converts various input formats into well-structured notes with diagrams",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm start",
    "client": "cd client && npm start",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "install-tailwind": "cd client && npm install -D tailwindcss postcss autoprefixer && npx tailwindcss init -p"
  },
  "dependencies": {
    "@genkit-ai/googleai": "^1.2.0",
    "axios": "^1.8.3",
    "concurrently": "^8.2.2",
    "firebase": "^11.4.0",
    "genkit": "^1.2.0"
  }
}
